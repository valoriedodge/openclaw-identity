# SPIRE certificates — generated locally, never commit private keys
spire-agent-certs/

# Audit logs
audit-logs/

# Policy — each deployment defines its own permissions
policy/

# Tracked services — each deployment manages its own gateways
.services

# SPIRE agent binary — download separately for your platform
spire-agent-tool

# Node
plugin/node_modules/
plugin/package-lock.json

# Python
__pycache__/
*.pyc
*.egg-info/

# macOS
.DS_Store
